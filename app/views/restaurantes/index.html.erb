<h2 class="center">Restaurantes Cadastrados</h2>
<br>
<table class="table table-striped table-bordered table-hover center">
	<tr>
		<th>Nome</th>
		<th>Endereço</th>
		<th>Especialidade</th>
	</tr>
<% @restaurantes.each do |restaurante| %>
	<tr>
		<td><%= restaurante.nome %></td>
		<td><%= restaurante.endereco %></td>
		<td><%= restaurante.especialidade %></td>
		<td><%= link_to 'Mostrar', action: 'show' %></td>
		<td><%= link_to 'Editar', action: 'edit'%></td>
		<td><%= link_to 'Deletar', action: 'destroy'%></td>
	</tr>
<% end %>
</table>
<br>
<b><%= link_to 'Novo Restaurante', action: 'new' %></b>


